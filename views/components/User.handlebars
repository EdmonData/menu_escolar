<div class="row g-4 mt-4">
      <div class="col">
        <div class="card">
          <div class="card-body text-start">
            <h5>M1: Menú Vegetariano</h5>
            <h5>M2: Menú Celiaco</h5>
            <h5>M3: Menú Estándar</h5>
          </div>
        </div>
      </div>
      <div class="col">
        <div class="card">
          <div class="card-body text-start">
            <h5>M4: Menú Calórico</h5>
            <h5>M5: Menú Autóctono</h5>
          </div>
        </div>
      </div>

      <table class="table table-bordered col-sm-12 col-md-6">
        <thead>
          <tr>
            <th scope="col">Fecha</th>
            <th scope="col">M1</th>
            <th scope="col">M2</th>
            <th scope="col">M3</th>
            <th scope="col">M4</th>
            <th scope="col">M5</th>
            <th scope="col">ACCIÓN</th>
          </tr>
        </thead>
        <tbody id="cuerpo">
          {{#each orders}}
          <tr class="bg-{{#if this.is_rectifield}}success{{else}}warning{{/if}}">
            <th>{{ dateFormat this.date "DD-MM-YYYY" }}</th>
            <td id="menu1_{{this.id}}">{{this.vegetarian}}</td>
            <td id="menu2_{{this.id}}">{{this.celiac}}</td>
            <td id="menu3_{{this.id}}">{{this.standar}}</td>
            <td id="menu4_{{this.id}}">{{this.caloric}}</td>
            <td id="menu5_{{this.id}}">{{this.ethnic}}</td>
            <td class="text-{{#if this.is_rectifield}}success font-weight-bold"><button type="submit"
                class="btn btn-success btn-outline-dark" onclick="verDetalle({{ this.id }})" >Ver Detalles</button></td>{{else}}danger
            font-weight-bold"><button type="submit" class="btn btn-warning btn-outline-dark" onclick="rectificar({{ this.id }})">Rectificar</button></td>
            {{/if}}

          </tr> 
          {{/each}}
        </tbody>
      </table>